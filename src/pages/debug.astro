---
import { getKV } from '../lib/kv';
let hasKV = false;
const kv = await getKV();
hasKV = !!kv;
---
<!DOCTYPE html>
<html>
<body style="background:#0d1117;color:#f0f6fc;padding:2rem;font-family:sans-serif;">
<p>hasKV (cloudflare:workers): {String(hasKV)}</p>
</body>
</html>
